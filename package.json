{
  "name": "studyverce",
  "private": true,
  "scripts": {
    "dev": "turbo dev --filter=web --filter=@studyverce/socket-server",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20"
  }
}
