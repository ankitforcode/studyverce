# Docker Compose ONLY — copy this file to repo root `.env` (NOT apps/web/.env.local)
# Get an authtoken: https://dashboard.ngrok.com/get-started/your-authtoken

NGROK_AUTHTOKEN=
# Reserved ngrok domain (must match NEXT_PUBLIC_APP_URL host in apps/web/.env.local)
NGROK_DOMAIN=patient-pika-evident.ngrok-free.app
